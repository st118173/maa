<body background="pp1.png">


<ul>
  <li>Admin Email :admin@ait.asia</li>
  <li>Admin Password : secret123</li>
  <li>Member Email :member@ait.asia</li>
  <li>Member Password : secret123</li>
</ul>

<%#= render "devise/shared/links" %>
<script>
$(function(){
$('.button-checkbox').each(function(){
var $widget = $(this),
$button = $widget.find('button'),
$checkbox = $widget.find('input:checkbox'),
color = $button.data('color'),
settings = {
on: {
icon: 'glyphicon glyphicon-check'
},
off: {
icon: 'glyphicon glyphicon-unchecked'
}
};

$button.on('click', function () {
$checkbox.prop('checked', !$checkbox.is(':checked'));
$checkbox.triggerHandler('change');
updateDisplay();
});

$checkbox.on('change', function () {
updateDisplay();
});

function updateDisplay() {
var isChecked = $checkbox.is(':checked');
// Set the button's state
$button.data('state', (isChecked) ? "on" : "off");

// Set the button's icon
$button.find('.state-icon')
.removeClass()
.addClass('state-icon ' + settings[$button.data('state')].icon);

// Update the button's color
if (isChecked) {
$button
.removeClass('btn-default')
.addClass('btn-' + color + ' active');
}
else
{
$button
.removeClass('btn-' + color + ' active')
.addClass('btn-default');
}
}
function init() {
updateDisplay();
// Inject the icon if applicable
if ($button.find('.state-icon').length == 0) {
$button.prepend('<i class="state-icon ' + settings[$button.data('state')].icon + '"></i> ');
}
}
init();
});
});
</script>
<script>
  $(document).ready(function(){

    $('html, body').animate({

      scrollTop: $("c").offset().top

    },1000);

  });
</script>
<!-- Button trigger modal -->

<!-- Modal -->
<div class="modal fade" id="loginmodal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" >
  <div class="modal-dialog" role="document" align="center">
    <div class="modal-content" align="center" style="height: 17cm">
      <div class="modal-header" align="center">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close" align="center"><span aria-hidden="true" align="center">&times;</span></button>
        <%= image_tag 'pp1.png', style: 'width: 2cm; height: 2cm' %>
        <h4 class="modal-title" id="myModalLabel">Maa Aapki Chaaya</h4>
      </div>
      <div class="modal-body" align="center">
        <h4 class="modal-title" id="myModalLabel">Login</h4>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="" id="login">

              <div class="row" style="margin-top:20px;float: left;width: 17cm">
                <div class="col-xs-1 col-sm-2 col-md-3 col-sm-offset-1 col-md-offset-3222222">
                  <form role="form" style="float: left">
                    <fieldset style=" float: left;width: 13cm" >

                      <hr class="colorgraph">
                      <div class="form-group">
                        <%= f.email_field :email,:id =>"email", autofocus: true,:class=>"form-control input-lg", :placeholder=>"Email Address" %>
                      </div>
                      <div class="form-group">
                        <%= f.password_field :password, :id=>"password",:class=>"form-control input-lg" ,:placeholder=>"Password" %>
                      </div>

                      <div class="field">
                        <%= captcha_tag  %>
                        <%= f.label :Enter_above_Captcha%>
                        <%= text_field_tag :captcha  %>
                      </div>

                      <span class="button-checkbox">
             <% if devise_mapping.rememberable? -%>

     <button type="button" class="btn" data-color="info">Remember Me</button>
                    <input type="checkbox" name="remember_me" id="remember_me" checked="checked" class="hidden">
                <div align="right">
               <%= render "devise/shared/links" %>
              </div>

            <% end -%>


				</span>
                      <hr class="colorgraph">
                      <div class="col-xs-6 col-sm-6 col-md-6">
                        <%= f.submit "Log in" ,:class=>"btn btn-lg btn-success btn-block", :value=>"Sign In"%>
                      </div>
                      <div class="col-xs-6 col-sm-6 col-md-6">
                        <%= link_to "Sign up", new_user_registration_path,:class=>"btn btn-lg btn-primary btn-block" %>

        <% end %>

        </div>

                    </fieldset>
                  </form>
                      </div>

                </div>
              </div>

            </div>





      </div>
    </div>
  </div>
</body>